@model IEnumerable<Event>

@{
    ViewBag.Title = "ByDate";
    ViewBag.PageId = "byDate";
}

<ul>
@foreach (Event e in Model) {
    <li>@Html.RouteLink(@e.Date.ToShortDateString(), "ByDate", new { slug = e.Location.Slug, date = @e.Date.ToSlug() })</li>    
}
</ul>
